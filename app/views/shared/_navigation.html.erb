<nav class="navbar navbar-default" role="navigation">
  <div class="container">
        <div class="navbar-header" style="width: 100%; vertical-align: top;">
            <div class="col-md-9">
            <h3 style="margin-top: 10px; margin-bottom: 5px">Invoicing Application</h3>
            </div>
            <div class="col-md-3" style="text-align: right">
              <a class="navbar-brand" href="/">Invoice</a>
                <% if user_signed_in? %>
                  <%= link_to('Logout', destroy_user_session_path, :method => :delete, :class => "navbar-brand") %>
                <% else %>
                  <%= link_to('Login', new_user_session_path, :class => "navbar-brand")  %>
                <% end %>
            </div>
        </div>
  <div>
</nav>